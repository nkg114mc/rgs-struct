package berkeleyentity.oregonstate

/**
 * The action queue that store all possible actions
 * for greedy search space, and can be updated after 
 * doing an action.
 * 
 * The queue can pop the best action
 */
class SearchStateCachedAction {
  
  def updateAction(action: SearchAction) {
    
  }
  
  def initialize() {
    
  }
  
  
}